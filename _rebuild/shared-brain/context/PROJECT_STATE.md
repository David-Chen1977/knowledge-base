# ⚠️ 已废弃

此文件已被 `state.json` 替代。

**新位置**: `context/state.json`
**更新方式**: `node scripts/sync-state.mjs <tool> <status> <task> [output]`

两个工具（OpenCode + WorkBuddy）统一通过 `state.json` 读写状态，不再维护三个独立的 MD 文件。
